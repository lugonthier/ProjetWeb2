<div class="row">
    <div class="col-md-8">
    <h2> Dernières photos</h2>
    <div class="row">
        <%= render @posts, cls: 'col-md-8'%>
    </div>
    </div>
    <div class="col-md-4">
    <h4>Filter</h4>
        <div class="list-group">
            <% @sport_categories.all.each do |categories| %>
                <%= link_to categories.name, categories_posts_path(slug: categories.slug), class: 'list-group-item'%>
            <% end %>
        </div>
        <h4>Nouveaux venu</h4>
        <div class="list-group">
            <%= render @users%>
        </div>
    </div>
</div>