<% content_for :content do %>

<div class="card">
    <div class="card-header">Créer un post</div>
        <div class=" card-body">

            <%= render 'form', post: @post %>
        </div>
    
</div>
<p class="d-flex p-2">
<a href="<%= posts_path %>" class="btn btn-primary"> Retour </a>
</p>
 <% end %>
 <%= render template: 'layouts/profile' %>