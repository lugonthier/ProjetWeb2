

<div class="container mb-6 ml-4">
<div class="row">
    <div class="col-8">
    <%= yield(:content) %>
    </div>
    <% if is_user_signed_in%>
    <div class="col-4">
        <div class="list-group">
            <a href="<%= profil_path %>" class="list-group-item">Mon compte</a>
            <a href="<%= sports_path %>" class="list-group-item">Mes sports</a>
            <a href="<%= me_posts_path %>" class="list-group-item">Mes photos</a>
            <a href="<%= posts_path %>" class="list-group-item">Mes abonnements</a>
            <% if current_user.admin  %>
                <a href="<%= admin_users_path %>" class="list-group-item">Gestion des utilisateurs</a>
                <a href="<%= admin_sports_path %>" class="list-group-item">Gestion des sports</a>
                <a href="<%= admin_sport_categories_path %>" class="list-group-item">Gestion des catégories</a>
                <a href="<%= admin_posts_path %>" class="list-group-item">Gestion des posts</a>
            <% end %>
        </div>
    </div>
  





    <% else %>
    <div class="col-4">
        <div class="list-group">
            <a href="<%= root_path %>" class="list-group-item">Accueil</a>
            <a href="<%= new_user_path %>" class="list-group-item">M'inscrire</a>
        </div>
    </div>
    <% end %>
</div>
</div>
