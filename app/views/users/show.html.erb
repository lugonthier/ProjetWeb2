<h1>Hey! <%= @user.username %></h1>
<h2>Your followers are</h2>
<% @followers.each do |follower| %>
  <p><%=User.find_by_id(follower.follower_id).firstname  %>
  
  </p>
<% end %>
​
<h2>You follow</h2>
<% @followees.each do |followee| %>
  <p><%= User.find_by_id(followee.followee_id).firstname %></p>
<% end %>
​
